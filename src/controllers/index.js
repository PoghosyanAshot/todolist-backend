"use strict";

const AuthController = require("./auth.controller");
const UserController = require("./user.controller");
const TodoController = require("./todo.contorller");

module.exports = {
    AuthController,
    UserController,
    TodoController,
};
